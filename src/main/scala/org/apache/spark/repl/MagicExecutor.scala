package org.apache.spark.repl


/**
 * Pull in the patches from spark-dynamic and put them here, I dare you.
 * Still needs a custom deploy to help it launch the JVM for the executor.
 * This is just what executes serialized classloader function references.
 */
class MagicExecutor {

}
