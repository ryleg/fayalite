/*
package org.fayalite.repl

import org.fayalite.util.SparkRef
import org.scalatest.FunSuite


/**
 * Created by ryle on 11/9/2014.
 */

/**
 * I'm not sure why this doesn't work and I don't really care.
 * Test it through the main method, that works fine from IJ
 */
class TestSparkREPLManager extends FunSuite {

  test("Evaluate x = 1 and print output of pseudo-repl") {
    //org.fayalite.repl.SparkREPLManager.main(Array(""))
    // this will throw exception if run here, but not if run there. crazy
  }
}


*/
